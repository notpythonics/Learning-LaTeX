\documentclass{article}

\begin{document}
Text can be \emph{emphasized}. % typesets in italic because this is the default way LaTeX emphasizes text

Besides from \textit{italics}, words can be \textbf{bold}, \textsl{slanted}, or typeset in \textsc{Small Caps}.

% you can combine font commands
Such commands can be \textit{\textbf{nested}}.

\emph{See how \emph{emphasizing} looks when nested.}
\end{document}
% bf stands for boldface
% \emph and \textit are basically the same!