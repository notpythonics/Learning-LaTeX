\documentclass{article}

\begin{document}
\tiny We \scriptsize start \footnotesize very \small small, \normalsize get \large big and \LARGE bigger, \huge huge and \Huge gigantic! \\

% normal size
\normalsize

% used groups/scopes
{\tiny Moamen} {\scriptsize likes} {\footnotesize hairless} {\small white} {\normalsize Asian} {\large women} {\LARGE so}{\huge o} {\Huge much}
\end{document}

Remember: everything after the document enviroment is ignored

Think of it this way: There is a property that holds the font size. 
When you use a command to the change the font size, it changes the property.
If there is a scope, the changes to the property are local to the scope.